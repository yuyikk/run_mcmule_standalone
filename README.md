# Readme
## Compilation && Running
Please modify the following line in the `CMakeLists.txt` and change the path to mcmule installation path
`set(MCMULE_LIB_PATH $ENV{MCMULE_PATH})`

There is a CMakeLists.txt in `cpp` and `fortran` folders.